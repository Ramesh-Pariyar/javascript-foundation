/* eslint-disable no-unused-vars */
var hi = "hi";
const bye = "bye";
var world = "world";

function helloWorld() {
  return `${hi} ${world}`;
}
const goodbyeWorld = {
  good: "good",
  bye,
  world,
};
